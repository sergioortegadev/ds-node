import { busquedaNumeroAuto } from "./BuscarNumeroAuto";

/* 
let numeros
let arrEstadisticas
for (numeros = 0; numeros < 10000; numeros++) {
 arrEstadisticas[]
 const element = array[index];
 
} */
console.log(busquedaNumeroAuto(1));
